#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long t;
    while(1)
    {
        float l;
        float PI=2*acos(0.0);
        cin>>l;
        if(l==0)
            break;
        cout<<fixed<<setprecision(2)<<(float)(l*l)/2*(float)(1/PI)<<endl;
        }
    return 0;
}
